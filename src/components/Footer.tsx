const CallToAction = () => {
  return <nav>CTA</nav>;
};

const Footer = () => {
  return (
    <section>
      <CallToAction />
      <footer>Footer</footer>
    </section>
  );
};

export default Footer;
